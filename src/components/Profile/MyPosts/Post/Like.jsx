

const Like = (props) => {
    //console.log (props.message);
    return (
       
            <div>
                <span>{props.count}</span>
            </div>
    

    );
}

export default Like;